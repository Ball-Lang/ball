# Self-host build artifacts (regenerated by the tools under
# dart/compiler/tool/compile_engine_{ts,cpp}.dart and
# dart/encoder/tool/roundtrip_engine.dart). Kept out of git so the
# repo doesn't balloon; CI regenerates them for validation.
engine.ball.json
engine.ball.pb
cli.ball.json
cli.ball.pb
# cli_core → C++ library header + its extracted Module ball-file
# (dart/compiler/tool/gen_cli_cpp.dart) — consumed by cpp/cli.
cli_module.ball.json
lib/cli_rt.h
lib/engine_rt.ts
lib/engine_smoke.ts
lib/engine_rt.cpp
lib/engine_rt/
lib/engine_roundtrip.dart
lib/*.backup
