//! `ball-encoder` — encodes Rust source into a Ball `Program` protobuf.
//!
//! This crate is currently an empty scaffold (Ball Phase 1a, issue #33).
//! `syn`-based AST-to-Ball mapping lands in Phase 3 (issues #42-43). See
//! `dart/encoder/lib/encoder.dart` for the reference implementation this
//! crate will port.
