# The self-hosted CLI core, compiled from dart/self_host/cli.ball.pb by
# `go run ./cmd/regen` (see AGENTS.md, issue #570). Gitignored like rust/cli's
# compiled_cli.rs and csharp/cli's CompiledCli.cs — a generated build artifact
# regenerated by CI, not committed. It only participates in the build under the
# `clicore` build tag.
compiled/compiled_cli.go
