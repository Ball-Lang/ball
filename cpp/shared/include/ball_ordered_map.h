#ifndef BALL_ORDERED_MAP_H
#define BALL_ORDERED_MAP_H

// Canonical definition lives in ball_dyn.h (embedded in compiler preamble).
// This header exists for non-embedded includes that need BallOrderedMap alone.
#include "ball_dyn.h"

#endif  // BALL_ORDERED_MAP_H
