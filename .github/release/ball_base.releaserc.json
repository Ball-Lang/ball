{
  "branches": ["main"],
  "tagFormat": "ball_base-v${version}",
  "plugins": [
    ["./tools/release/only-package-commits.mjs", { "paths": ["dart/shared"] }],
    ["@semantic-release/changelog", { "changelogFile": "dart/shared/CHANGELOG.md" }],
    ["@semantic-release/exec", {
      "prepareCmd": "node tools/release/set_manifest_version.mjs --format=pubspec --file=dart/shared/pubspec.yaml --version=${nextRelease.version}",
      "publishCmd": "gh workflow run release-publish.yml --ref ball_base-v${nextRelease.version}"
    }],
    ["@semantic-release/git", {
      "assets": ["dart/shared/pubspec.yaml", "dart/shared/CHANGELOG.md"],
      "message": "chore(release): ball_base ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
    }],
    ["@semantic-release/github", { "successComment": false }]
  ]
}
