name: ball_engine
environment:
  sdk: ^3.9.0
resolution: workspace

dependencies:
  ball_base:
    path: ../shared
  ball_resolver:
    path: ../resolver

dev_dependencies:
  ball_encoder:
    path: ../encoder
  test: ^1.25.0
