<!-- Parent: ../AGENTS.md -->

# web

## Purpose
Container for the standalone browser-based Ball playground (separate from the Jaspr website).

## Subdirectories

| Directory | Description |
|-----------|-------------|
| `playground/` | Self-contained HTML/JS browser playground for running Ball programs in-browser |

## For AI Agents

- All meaningful content lives in `web/playground/` — see `web/playground/AGENTS.md`.
- Do not add files directly to `web/`; they belong in the subdirectory.
