# Upstream protobuf conformance — expected failures for ball_protobuf.
#
# Format: one fully-qualified conformance test name per line; '#' starts a
# comment. The conformance_test_runner treats every name here as an EXPECTED
# failure and fails the suite if such a test unexpectedly PASSES — keep this
# list exact (prune a line the moment its test starts passing).
#
# Scope: the harness registers the `TestAllTypes*` messages of all three
# conformance families — protobuf_test_messages.{proto2,proto3,editions} — from
# a protoc FileDescriptorSet and drives ball_protobuf's feature-aware
# binary/JSON codecs (including the well-known types, oneofs, message merge,
# unknown-field retention, and the proto3-JSON strictness rules).
#
# Status: ALL registered tests pass (2769 successes, 0 expected failures). The
# ~2800 reported "skipped" are text-format tests and unregistered message
# types, which this harness does not exercise.
#
# This list is intentionally empty.
