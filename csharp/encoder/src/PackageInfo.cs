namespace Ball.Encoder;

/// <summary>
/// Phase 1 (#378) scaffold marker. The real C# -> Ball encoder (Roslyn AST
/// walk, routed entirely through universal std per
/// .claude/skills/new-ball-language/SKILL.md Phase 3) lands in issue #382.
/// </summary>
public static class PackageInfo
{
    public const string Name = "Ball.Encoder";
}
