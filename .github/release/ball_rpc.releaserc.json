{
  "branches": ["main"],
  "tagFormat": "ball_rpc-v${version}",
  "plugins": [
    ["./tools/release/only-package-commits.mjs", { "paths": ["dart/ball_rpc"] }],
    ["@semantic-release/changelog", { "changelogFile": "dart/ball_rpc/CHANGELOG.md" }],
    ["@semantic-release/exec", {
      "prepareCmd": "node tools/release/set_manifest_version.mjs --format=pubspec --file=dart/ball_rpc/pubspec.yaml --version=${nextRelease.version}",
      "publishCmd": "gh workflow run release-publish.yml --ref ball_rpc-v${nextRelease.version}"
    }],
    ["@semantic-release/git", {
      "assets": ["dart/ball_rpc/pubspec.yaml", "dart/ball_rpc/CHANGELOG.md"],
      "message": "chore(release): ball_rpc ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
    }],
    ["@semantic-release/github", { "successComment": false }]
  ]
}
