# The self-hosted engine, compiled from dart/self_host/engine.ball.pb by
# `go run ./cmd/regen` (see AGENTS.md). Gitignored like rust/engine's
# compiled_engine.rs and csharp/engine's CompiledEngine.cs — a ~200k-line build
# artifact regenerated by CI, not committed. It only participates in the build
# under the `selfhost` build tag.
compiled/compiled_engine.go
