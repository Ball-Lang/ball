{
  "branches": ["main"],
  "tagFormat": "ball_engine-v${version}",
  "plugins": [
    ["./tools/release/only-package-commits.mjs", { "paths": ["dart/engine"] }],
    ["@semantic-release/changelog", { "changelogFile": "dart/engine/CHANGELOG.md" }],
    ["@semantic-release/exec", {
      "verifyReleaseCmd": "node tools/release/lockstep_plan.mjs --record --package=ball_engine --version=${nextRelease.version} --type=${nextRelease.type}",
      "prepareCmd": "node tools/release/set_manifest_version.mjs --format=pubspec --file=dart/engine/pubspec.yaml --version=${nextRelease.version} && node tools/release/sync_pubspec_deps.mjs --workspace-root=.",
      "publishCmd": "gh workflow run release-publish.yml --ref ball_engine-v${nextRelease.version}"
    }],
    ["@semantic-release/git", {
      "assets": ["dart/engine/pubspec.yaml", "dart/*/pubspec.yaml", "dart/engine/CHANGELOG.md"],
      "message": "chore(release): ball_engine ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
    }],
    ["@semantic-release/github", { "successComment": false }]
  ]
}
