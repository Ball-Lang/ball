// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ball/v1/ball.proto
// Protobuf Java Version: 4.35.1

package com.ball.v1;

@com.google.protobuf.Generated
public interface CallSiteOrBuilder extends
    // @@protoc_insertion_point(interface_extends:ball.v1.CallSite)
    com.google.protobuf.MessageOrBuilder {

  /**
   * <pre>
   * The user module containing the call.
   * </pre>
   *
   * <code>string module = 1 [json_name = "module"];</code>
   * @return The module.
   */
  java.lang.String getModule();
  /**
   * <pre>
   * The user module containing the call.
   * </pre>
   *
   * <code>string module = 1 [json_name = "module"];</code>
   * @return The bytes for module.
   */
  com.google.protobuf.ByteString
      getModuleBytes();

  /**
   * <pre>
   * The user function containing the call.
   * </pre>
   *
   * <code>string function = 2 [json_name = "function"];</code>
   * @return The function.
   */
  java.lang.String getFunction();
  /**
   * <pre>
   * The user function containing the call.
   * </pre>
   *
   * <code>string function = 2 [json_name = "function"];</code>
   * @return The bytes for function.
   */
  com.google.protobuf.ByteString
      getFunctionBytes();

  /**
   * <pre>
   * The base module being called (e.g. "std_fs").
   * </pre>
   *
   * <code>string callee_module = 3 [json_name = "calleeModule"];</code>
   * @return The calleeModule.
   */
  java.lang.String getCalleeModule();
  /**
   * <pre>
   * The base module being called (e.g. "std_fs").
   * </pre>
   *
   * <code>string callee_module = 3 [json_name = "calleeModule"];</code>
   * @return The bytes for calleeModule.
   */
  com.google.protobuf.ByteString
      getCalleeModuleBytes();

  /**
   * <pre>
   * The base function being called (e.g. "file_read").
   * </pre>
   *
   * <code>string callee_function = 4 [json_name = "calleeFunction"];</code>
   * @return The calleeFunction.
   */
  java.lang.String getCalleeFunction();
  /**
   * <pre>
   * The base function being called (e.g. "file_read").
   * </pre>
   *
   * <code>string callee_function = 4 [json_name = "calleeFunction"];</code>
   * @return The bytes for calleeFunction.
   */
  com.google.protobuf.ByteString
      getCalleeFunctionBytes();

  /**
   * <pre>
   * The module that DECLARES the callee, when it differs from callee_module.
   *
   * The engine dispatches a base call by function identity, not by the
   * call-site module string: an unqualified call (empty module) or one naming a
   * benign-looking module still reaches the declaring module's base function.
   * callee_module keeps what the program wrote; this field names where that
   * call actually lands, so a machine-readable report cannot be misled by the
   * call-site spelling (issue #609). Empty when the call site named the
   * declaring module itself, and for every std capability (whose #402 bare-name
   * resolution is already unambiguous by function name alone).
   * </pre>
   *
   * <code>string resolved_module = 5 [json_name = "resolvedModule"];</code>
   * @return The resolvedModule.
   */
  java.lang.String getResolvedModule();
  /**
   * <pre>
   * The module that DECLARES the callee, when it differs from callee_module.
   *
   * The engine dispatches a base call by function identity, not by the
   * call-site module string: an unqualified call (empty module) or one naming a
   * benign-looking module still reaches the declaring module's base function.
   * callee_module keeps what the program wrote; this field names where that
   * call actually lands, so a machine-readable report cannot be misled by the
   * call-site spelling (issue #609). Empty when the call site named the
   * declaring module itself, and for every std capability (whose #402 bare-name
   * resolution is already unambiguous by function name alone).
   * </pre>
   *
   * <code>string resolved_module = 5 [json_name = "resolvedModule"];</code>
   * @return The bytes for resolvedModule.
   */
  com.google.protobuf.ByteString
      getResolvedModuleBytes();
}
