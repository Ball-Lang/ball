name: ball_base
description: >-
  Cross-language standard library for the Ball programming language with
  protobuf types and std module builders shared by all compilers and engines.
version: 0.1.0
homepage: https://github.com/ball-lang/ball
repository: https://github.com/ball-lang/ball
issue_tracker: https://github.com/ball-lang/ball/issues
topics:
  - programming-language
  - protobuf
  - compiler
  - standard-library
  - ball

environment:
  sdk: ^3.9.0

dependencies:
  fixnum: ^1.1.1
  protobuf: ^6.0.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0




resolution: workspace
