# Ball Self-Host Status

Tracks the round-trip story for the reference Dart engine across all
target languages: encode the live engine to Ball IR, compile back to
each supported language, run conformance.

Last refreshed: 2026-06-18

## Pipeline

| Stage | Tool | Output |
|-------|------|--------|
| 1. Encode | `dart run dart/encoder/tool/roundtrip_engine.dart` | `dart/self_host/engine.ball.pb`, `dart/self_host/lib/engine_roundtrip.dart` |
| 2. C++ compile | `dart run dart/compiler/tool/compile_engine_cpp.dart` | `dart/self_host/lib/engine_rt.cpp` |
| 3. TS compile | `ts/compiler` (strip `@type`, compile) | `ts/engine/src/compiled_engine.ts` |
| 4. Conformance -- Dart roundtrip | `dart test dart/self_host/test/engine_parity_test.dart` | parity vs live engine |
| 5. Conformance -- C++ self-host | `cmake --build cpp/build --target test_selfhost_conformance` | per-fixture pass/fail |
| 6. Conformance -- TS self-host | `cd ts/engine && npm test` | per-fixture pass/fail |

## Per-language status

### Dart engine (reference)

**288 / 288 conformance pass (100%).** The Dart tree-walking interpreter is the
reference implementation against which all other engines are validated.

### Dart roundtrip (live engine to Ball IR to Dart)

**Compile state:** `dart/self_host/lib/engine_roundtrip.dart` regenerates with
0 `dart analyze` errors. The round-trip is clean.

**Parity:** All non-skipped conformance fixtures pass. 6 fixtures are skipped
because they depend on `BallEngine` constructor knobs (`timeoutMs`,
`maxMemoryBytes`, `sandbox`, etc.) that the IR cannot express.

### TypeScript self-host

**302 / 302 pass (100%)** -- 284 conformance fixtures + 18 unit tests.
The self-hosted engine (`ts/engine/src/compiled_engine.ts`) is generated by
compiling `dart/self_host/engine.ball.json` through `@ball-lang/compiler`.
`index.ts` wraps it with proto3 JSON normalization (`protoWrap`), method
dispatch handlers, and std function registrations.

4 conformance fixtures are skipped (host-knob tests: `196_timeout`,
`197_memory_limit`, `201_input_validation`, `202_sandbox_mode`).

### C++ engine + compiler

**283 / 283 conformance pass (100%).** The C++ compiler compiles each
`tests/conformance/*.ball.json` fixture to C++ source; the compiled output
runs and matches golden output for all 288 fixtures minus 4 host-knob tests
(no golden output) and 1 var-capture carve-out (`312`, #69).

Key capabilities:
- Dynamic method dispatch + generics erasure
- Closure conversion
- Map reference semantics (insertion-ordered)
- All std-lib codecs (std, std_collections, std_io, std_memory)
- Pattern matching (switch_expr, relational/logical-and patterns)
- Factory constructors with `identical()` support
- Cascade write-back

### C++ self-host (compiled engine_rt.cpp)

The C++ self-hosted engine (compiled from `engine.ball.json` via the C++
compiler) passes EVERY conformance fixture (0 fail, 0 timeout, 0 crash).
Each fixture is its own CTest test, run in its own process, so a crash or
hang fails only that fixture. The former OOP self-binding blockers (setter
dispatch, virtual-dispatch scope issues) are resolved.

## ball_protobuf cross-target compilation

`ball_protobuf` (the pure-Dart, descriptor-driven protobuf runtime with
Editions support) compiles to both target languages:

| Target | Lines | Status |
|--------|-------|--------|
| TypeScript | ~4319 | marshal/unmarshal proven, compileModule() works |
| C++ | ~8456 | g++ clean, compile_library() works |

Phase 5 (cross-target library mode) is DONE: `ts/compiler` `compileModule()`
and `cpp/compiler` `compile_library()` both produce working library output.

The Editions resolver (`edition.dart` + `editions.dart`) runs identically on
all three engines -- see `tests/editions/portability_matrix.md` for the proof.

## Skip-list

Conformance fixtures that depend on `BallEngine` constructor knobs that don't
survive the round-trip:

- `196_timeout` -- needs `timeoutMs:` argument
- `197_memory_limit` -- needs `maxMemoryBytes:`
- `201_input_validation` -- needs `maxModules:` / `maxExpressionDepth:` / `maxProgramSizeBytes:`
- `202_sandbox_mode` -- needs `sandbox: true`
