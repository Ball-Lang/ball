# Compiled Ball artifacts — build outputs, not part of ball_base's published
# API. The protobuf engine now ships as the `ball_protobuf` source package;
# these are its compiled facade-Module form (regenerated by
# dart/encoder/bin/gen_ball_protobuf.dart) and are kept here only as a build
# convenience, not shipped to pub.dev.
ball_protobuf.json
ball_protobuf.bin
